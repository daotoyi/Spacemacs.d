\usepackage[colorlinks,linkcolor=black,anchorcolor=blue,citecolor=cyan]{hyperref}

%% temporary text
\usepackage{lipsum}

%% shade color 
\colorlet{shadecolor}{blue!20}

%% customize box background of text
\usepackage{xcolor}
\usepackage{framed}
\usepackage{tcolorbox}
\newtcbox{\HL}[1][red]
  {on line, arc = 0pt, outer arc = 0pt,
   colback = #1!10!white, colframe = #1!50!black,
   boxsep = 0pt, left = 1pt, right = 1pt, top = 2pt, bottom = 2pt,
   boxrule = 0pt}

%% format code
\usepackage{listings,xcolor,caption}
\lstset{
  language=Python,                  % 设置语言
  basicstyle=\ttfamily,                % 设置字体族
  basicstyle=\footnotesize\tiny,      % size of fonts used for the code; 
  breaklines=true,                     % 自动换行
  morekeywords={},                     % 设置更多的关键字，用逗号分隔
  emph={self},                         % 指定强调词，如果有多个，用逗号隔开
  emphstyle=\bfseries\color{Rhodamine},              % 强调词样式设置
  keywordstyle=\bfseries\color{red},                 % 设置关键字为粗体，颜色为 NavyBlue
  commentstyle=\itshape\color{black!50!white},       % 设置注释样式，斜体，浅灰色
  stringstyle=\bfseries\color{PineGreen!90!black},   % 设置字符串样式
  columns=flexible,
  numbers=left,                        % 显示行号在左边
  numbersep=1.5em,                       % 设置行号的具体位置
  numberstyle=\footnotesize,           % 缩小行号
  frame=single,                        % none, shadowbox,single边框;leftline,topline,rightline, bottomline
  framesep=1em,                         % 设置代码与边框的距离
  frameround=tttt                      % 代码框： 圆角
}
